{% extends 'base.html.twig' %}

{% block body %}

    <h1 style="text-align: center"> Ajout d'un nouveau programme </h1>
    <div class="container" style="margin-bottom: 10px; margin-top: 15px">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-lg btn-primary" type="submit" style="margin-top: 10px"> Ajouter </button>
        {{ form_end(form) }}
    </div>

{% endblock %}

